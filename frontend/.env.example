# Copy this file to .env and fill in values for your environment.

# Base URL for all API requests (Vite dev server proxies /api → backend)
VITE_API_BASE_URL=/api

# Optional: override API timeout in milliseconds (default: 15000)
# VITE_API_TIMEOUT=15000
